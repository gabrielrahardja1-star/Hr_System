"""Site agent — runs on the PC the fingerprint terminal is cabled to.

Phase 1 ships only `probe_device.py`, the on-site connectivity test. The full
poller + spool + sender arrives in Phase 4, once the ingest contract is proven.
"""
