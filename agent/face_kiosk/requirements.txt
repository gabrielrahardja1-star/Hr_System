# Face-recognition kiosk — a punch source that watches a webcam instead of a
# fingerprint terminal. Runs on the kiosk Mac ONLY. The HQ server does not need
# any of this (same rule as agent/requirements.txt).
#
#   python3.14 -m venv agent/face_kiosk/.venv
#   agent/face_kiosk/.venv/bin/pip install -r agent/face_kiosk/requirements.txt
#   agent/face_kiosk/.venv/bin/python -m facekiosk.fetch_models

opencv-python>=5.0        # bundles FaceDetectorYN (YuNet) + FaceRecognizerSF (SFace)
numpy>=2.0
cryptography>=42          # Fernet — encrypts the local face gallery at rest

# Standalone web app (facekiosk.app)
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1

# Phase 2 (HQ wiring) will add:
# httpx>=0.27             # POST batches to /api/v1/punches, same as the pyzk agent
